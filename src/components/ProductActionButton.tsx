'use client';

import React from 'react';

export const ProductActionButton: React.FC = () => {
  return (
    <div>
      <button type="button">
        Custom Action
      </button>
    </div>
  );
};